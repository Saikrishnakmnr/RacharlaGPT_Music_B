(()=>{const el=document.getElementById('offer'),cfg=window.RGM_CONFIG||{};if(!el||!cfg.PUBLIC_API_BASE||cfg.PUBLIC_API_BASE.includes('YOUR-'))return;fetch(cfg.PUBLIC_API_BASE.replace(/\/$/,'')+'/pricing').then(r=>r.json()).then(d=>{if(d.campaign_active){el.innerHTML=`<b>${d.campaign_name}</b><del>₹${d.normal_price}</del><strong>₹${d.price}</strong><span>limited-time offer</span>`}else{el.innerHTML='<b>Personalized song</b><strong>₹'+d.normal_price+'</strong><span>payment required to place an order</span>'}}).catch(()=>{});})();
